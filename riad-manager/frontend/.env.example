# Frontend Environment Variables
# Copy this file to .env.local and fill in your values

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:5001/api

# Admin JWT token (obtain from POST /api/auth/login)
# Store securely — never commit actual tokens
NEXT_PUBLIC_ADMIN_EMAIL=admin@riad.ma
